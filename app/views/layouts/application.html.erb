<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://fonts.googleapis.com/css?family=Megrim" rel="stylesheet", href="https://fonts.googleapis.com/css?family=Open+Sans" rel="stylesheet">

    <title><%= content_for?(:title) ? yield(:title) : "BLOOM" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar" >
      <ul class="title-area">
        <li class="title">
          <a href="/">bloom</a>
        </li>
      </ul>

      <section class="top-bar-section">
        <ul class="right">
          <li class="sign-in">
            <a class="sign-in-info" href="#">
              <% if current_user  %>
              <span>
                <h6>Signed in as <%= "#{current_user.first_name} #{current_user.last_name}" %></h6>
                <%= link_to "Logout", logout_path %>
              </span>
              <% else %>
              <%= link_to "Sign in with Google", "/auth/google" %>
              <% end %>
            </a>
          </li>
        </ul>
      </section>

    </nav>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
